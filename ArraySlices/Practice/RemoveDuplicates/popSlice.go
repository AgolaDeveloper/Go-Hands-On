// this file acts as our data structure for our practice program
// it creates a struct that we'll use to store our basic data [types] elements
package main

func slice() []int {
	//this function creates our basic data struct[slice in this case]
	ourSlice := make([]int, 0)
	return ourSlice
}
