// this is the data structure that stores all our contact details [for now]
package main

//func that creates creates the struct

//it takes nothing as a parameter since it does use any imporetd return values
func phonebook() map[string]string {
	//creating an empty map ready to store contact details
	phoneBook := make(map[string]string)

	//in case the func is called it returns the phoneBook Struct [map]
	return phoneBook
}
