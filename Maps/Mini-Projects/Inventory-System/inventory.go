// this file creates our simple data struct [map for now]
package main

//the function creates a map as our Data Structure for this project
//it returns a map
func ourStruct() map[string]map[int]int {
	//creating nested map
	//map with another map as its value

	theStruct := make(map[string]map[int]int)

	return theStruct
}
